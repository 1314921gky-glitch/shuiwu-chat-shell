# Cursor 改版任务 R3（Grok 4.6 / Extra High）

请用 **Cursor Grok 4.6 + Effort Extra High** 修改本仓库 `/workspace/shuiwu-chat-shell`。  
实现细节与 UI 由你决定；下面是产品需求。不要接 LLM API。不要无关大重构。

## 需求：聊天上要体现时间（精确到秒）

当前项目聊天时间线（用户气泡、系统等待、交付块）**没有可读的发送/交付时间**，或不够精确。

### 必须做到
1. **每一轮用户消息气泡**上清晰展示该轮发送时间，格式精确到秒，例如：`2026-09-10 22:15:33`（或同等可读、含秒的中文习惯格式；时区按本地）。
2. **系统/等待气泡、交付成品块**也要有对应时间（写入 INPUT 的时间 / 交付出现的时间），精确到秒。
3. 数据源优先用已有 `createdAt`（及 OUTPUT 侧可用的 mtime / 解析时间）；若 UI 缺字段，补齐类型与扫描逻辑，保证刷新后时间仍正确。
4. 深浅色主题下时间文字都清晰可读（次要信息样式即可，但必须看得见）。
5. 左侧项目列表若展示「最近更新」，也建议精确到秒（或至少分），与聊天一致。

### 非目标
- 不要改坚果云管道目录约定（`01_INPUT` / `02_OUTPUT` / `03_ARCHIVE`）。
- 不要为了展示时间去改任务文件夹命名规则（仍可按日命名）；时间展示走 `createdAt`/文件时间即可。
- 不要接外部时钟服务。

## 交付
1. 改完后更新 README 一句说明「聊天时间精确到秒」。
2. `npm test` 全部通过；必要时补一条与时间格式相关的单测。
3. 在本仓库写短报告 `CURSOR_TASK_R3_DONE.md`：改了哪些文件、时间从哪取、如何肉眼验证。

成功标准：打开任一已有项目聊天，每条用户消息与交付块都能看到精确到秒的时间。
