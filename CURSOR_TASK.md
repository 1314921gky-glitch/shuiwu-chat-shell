# Cursor 任务：水务AI聊天生图壳（Windows EXE）

请用当前模型 **Grok 4.6 / Extra High（xhigh）** 完成。  
**技术选型、GitHub 参考项目评判、UI 框架选择，全部由你自行调研并决策**（不要等我指定 Tauri/WPF/Electron）。选完后在 README 写清：对比了哪些项目、为什么选这个、为什么不选别的。

## 背景（现有链路，不要改底层）
同事在 Windows 坚果云工作站丢任务文件夹 → 同步到 box → waterworks 监测 → Antigravity 生图 → 回写 OUTPUT。

Windows 工作站根目录（真实机）：
`C:\Users\Administrator\Nutstore\1\我的坚果云\水务AI工作站`

结构：
- `01_待处理任务_INPUT`：新建任务文件夹放这里（禁止在这一层直接丢散文件）
- `02_已完成交付_OUTPUT`：成品图 + `交付说明.txt`
- `03_历史归档_ARCHIVE`：归档

INPUT 说明原文：请先新建任务文件夹（例如：`20260910_乐流泵房美化_周雨琪`），再把图纸和 `需求.txt` / 录音放进去。  
命名示例：`20260910_1_龚魁彦_示例项目`；夹内至少 `需求.txt`（UTF-8）+ 附件图。

## 产品目标
做一个**双击 EXE**，放到水务工作站**根目录**。同事感觉像在聊天生图：
1. 可传附件（原图/图纸）
2. 可打字聊天
3. 点发送后等待
4. 底层逻辑不变：把文件归类写进坚果云 INPUT，由现有管道生图；成品出现在 OUTPUT 后在聊天里展示

要求：简洁、高大上、设计做好。不要做完整 LLM 客户端（不要接 API 生图），只做「聊天壳 + 文件夹编排 + 轮询交付」。

## 你要做的事
1. **自己**搜并评判 GitHub 上做得好的聊天/附件桌面项目，再选型落地。
2. 实现 Windows 可双击运行的程序（优先可发布为 EXE 或小目录分发）。
3. 中文 UI；自动探测工作站根（EXE 旁若有 `01_待处理任务_INPUT` 优先；否则常见 Nutstore 路径；否则设置里可改）。
4. 发送时原子写入：`YYYYMMDD[_seq]_简称_操作人/` → `需求.txt` + 附件拷贝 → 放进 INPUT。
5. 轮询 OUTPUT 同名任务夹，出图后聊天气泡展示；可一键打开 OUTPUT 文件夹。
6. README（中文）：用法、目录映射、如何把 EXE 放到工作站根目录、如何本地构建。
7. 在本仓库完成可构建状态；能说明 Windows 打包步骤。若当前 Linux 环境打不出 Windows EXE，请把源码与构建脚本做完整，并给出在 Windows 上一条龙打包命令。

## 成功标准
- 同事双击即可用，不需要理解 INPUT/OUTPUT 结构
- 发送后 INPUT 出现规范任务夹
- OUTPUT 出现成品后聊天能看到图
- README 含你的选型评判结论
