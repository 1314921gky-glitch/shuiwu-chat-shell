# Cursor 改版任务 R2（Grok 4.6 / Extra High）

请用当前模型 **Cursor Grok 4.6 + Effort Extra High** 改 `/workspace/shuiwu-chat-shell`。  
**你自己决定实现细节与 UI 结构**；下面是产品需求，不要问我选组件库。

## 缺陷 1：主题要能亮也能暗
- 支持 **浅色 / 深色** 两套皮肤（可跟随系统 + 手动切换）。
- 顶栏或设置里有明确切换入口；偏好持久化到本地设置。
- 两套都要干净、水务感（别只是反色糊弄）。

## 缺陷 2：项目是持续对话，不是一锤子买卖（更重要）
当前问题：像一次性填表发送就结束；没有「新建项目」；选中已有项目不能继续改。

### 信息架构
1. **项目列表**（左侧或顶层）：列出已有项目（来自 INPUT / OUTPUT / ARCHIVE 扫描重建 + 本地会话状态）。
2. **「新建项目」按钮**（必须有）：只有点新建时，才出现现在这种「创建字段」——项目简称、操作人、需求、附件等。
3. **选中某个已有项目**后：进入该项目的 **持续聊天**——
   - 可继续打字提调整需求
   - 可继续添加附件
   - 每次发送 = 对该项目的一次新的迭代投递（仍走坚果云管道，底层逻辑不变）
   - 聊天时间线保留该项目历史（用户消息、等待、成品图、交付说明）
4. 新建流程走完第一次发送后，该项目进入列表并可再次打开继续聊。

### 与坚果云管道的映射（保持兼容，可增强）
- 第一次创建：仍写入 `01_待处理任务_INPUT/<任务名>/`（`需求.txt` + 附件），命名规则可沿用现有。
- **同一项目后续调整**：不要假装没项目。推荐（你可微调但需在 README 写清）：
  - 方案 A：同名任务夹内追加 `需求_2.txt` / `迭代_002/` 或带时间戳的迭代子目录 + 附件，并在会话里展示轮次；或
  - 方案 B：每次迭代生成 `YYYYMMDD_seq_简称_操作人` 的新夹，但 UI 上归到同一「项目」线程（本地维护 projectId ↔ 多次 taskFolder 映射）。
- 轮询 `02_已完成交付_OUTPUT`（及归档）时，把对应轮次成品挂回**同一项目聊天**，而不是开新无关会话。
- 禁止在 INPUT 根层丢散文件；继续原子写入。

### UX
- 未选项目且未点新建时：主区提示「请选择项目或新建项目」，**不要**直接甩出整套创建表单当唯一界面。
- 新建：表单 + 首次发送。
- 已有项目：聊天气泡 + 底部输入（附件+文字+发送）持续迭代。
- 中文 UI。

## 交付
1. 改完更新 README（主题、新建/持续项目、迭代如何落盘）。
2. `npm test` 通过；能说明如何验证「新建 → 出图 → 再对该项目追加需求」。
3. 不要接 LLM API。不要无关大重构炫耀。

成功标准：同事能新建项目；能点进旧项目继续用聊天+附件改图；能切换深浅色。
